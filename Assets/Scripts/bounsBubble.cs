using UnityEngine;

public class BounsBubble : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public float size = 2.0f;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
